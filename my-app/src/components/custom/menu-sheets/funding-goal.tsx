import { FundPool } from "@/types/fund-pool";

interface FundingGoalProps {
  fundPool: FundPool;
  onClose: () => void
}

export default function FundingGoal({ fundPool, onClose }: FundingGoalProps) {
  return (
    <div className="flex h-full flex-col">
      
    </div>
  )
}