import { Profile } from "@/types/profile";

interface InvestorIndustriesProps {
  profile: Profile;
  onClose: () => void
}

export default function InvestorIndustries({ profile, onClose }: InvestorIndustriesProps) {
  return (
    <div className="flex h-full flex-col">
      
    </div>
  )
}