import { Startup } from "@/types/startup";

interface StartupManageTeamProps {
  startup?: Startup;
  onClose: () => void
}

export default function StartupManageTeam({ startup, onClose }: StartupManageTeamProps) {
  return (
    <div className="flex h-full flex-col">
      
    </div>
  )
}