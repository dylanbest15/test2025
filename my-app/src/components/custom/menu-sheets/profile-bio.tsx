import { Profile } from "@/types/profile";

interface ProfileBioProps {
  profile: Profile;
  onClose: () => void
}

export default function ProfileBio({ profile, onClose }: ProfileBioProps) {
  return (
    <div className="flex h-full flex-col">
      
    </div>
  )
}