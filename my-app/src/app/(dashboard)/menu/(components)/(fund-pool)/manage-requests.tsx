import { FundPool } from "@/types/fund-pool";

interface ManageRequestsProps {
  fundPool: FundPool;
  onClose: () => void
}

export default function ManageRequests({ fundPool, onClose }: ManageRequestsProps) {
  return (
    <div className="flex h-full flex-col">
      
    </div>
  )
}